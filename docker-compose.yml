version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./faiss_index.bin:/app/faiss_index.bin
      - ./faiss_index.bin_meta.pkl:/app/faiss_index.bin_meta.pkl
